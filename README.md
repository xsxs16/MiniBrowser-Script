# MiniBrowser-Script
MiniBrowser is a lightweight web browser for Scriptable on iOS with optional 4-digit passcode protection. It automatically adds https:// to URLs and saves all visited pages in a log history
